/**
 * Fixed UUID for the default "Tenant Admin" role created in each tenant DB during provisioning.
 * Used when assigning the role to the first user (self-signup) and when creating the role in setupDefaultData.
 */
export const TENANT_ADMIN_ROLE_ID = 'f0000001-0001-0001-0001-000000000001';
